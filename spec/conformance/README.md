# Conformance

The public conformance suite is future work. For now, use the C++ tests in `tests/` and the generic checklists in `checklists/` as regression coverage for the reference implementation.

Planned conformance material should stay generic, deterministic, and independent of private asset packs.
